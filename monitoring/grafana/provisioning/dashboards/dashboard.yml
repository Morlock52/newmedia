apiVersion: 1

providers:
  - name: 'Media Server Dashboards'
    orgId: 1
    folder: 'Media Server'
    folderUid: media-server
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
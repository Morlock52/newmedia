/**
 * Enhanced Media Server API
 * Production-ready Express.js API with Socket.IO integration and comprehensive error handling
 */

const express = require('express');
const cors = require('cors');
const helmet = require('helmet');
const rateLimit = require('express-rate-limit');
const { createServer } = require('http');
const { Server } = require('socket.io');
const compression = require('compression');
const morgan = require('morgan');
const Joi = require('joi');
const fs = require('fs').promises;
const path = require('path');
const axios = require('axios');
const Docker = require('dockerode');

class EnhancedMediaServerAPI {
    constructor() {
        this.app = express();
        this.server = createServer(this.app);
        this.io = new Server(this.server, {
            cors: {
                origin: process.env.CORS_ORIGIN || "*",
                methods: ["GET", "POST", "PUT", "DELETE"],
                credentials: true
            },
            transports: ['websocket', 'polling']
        });
        
        this.port = process.env.API_PORT || 3004;
        this.docker = new Docker();
        this.wsClients = new Map();
        this.serviceCache = new Map();
        this.rateLimitStore = new Map();
        
        this.initialized = false;
        this.monitoringInterval = null;
        
        this.setupMiddleware();
        this.setupRoutes();
        this.setupSocketIO();
        this.setupErrorHandling();
    }

    async initialize() {
        try {
            // Test Docker connection
            await this.docker.ping();
            
            // Initialize service discovery
            await this.discoverServices();
            
            this.initialized = true;
            console.log('✅ Enhanced Media Server API initialized');
        } catch (error) {
            console.error('❌ API initialization failed:', error.message);
            throw error;
        }
    }

    setupMiddleware() {
        // Security headers
        this.app.use(helmet({
            contentSecurityPolicy: {
                directives: {
                    defaultSrc: ["'self'"],
                    scriptSrc: ["'self'", "'unsafe-inline'", "'unsafe-eval'"],
                    styleSrc: ["'self'", "'unsafe-inline'"],
                    imgSrc: ["'self'", "data:", "https:"],
                    connectSrc: ["'self'", "ws:", "wss:"]
                }
            },
            crossOriginEmbedderPolicy: false
        }));\n        
        // CORS
        this.app.use(cors({
            origin: (origin, callback) => {\n                // Allow requests with no origin (mobile apps, Postman, etc.)\n                if (!origin) return callback(null, true);\n                \n                const allowedOrigins = [\n                    'http://localhost:3000',\n                    'http://localhost:3001',\n                    'http://localhost:8080',\n                    'http://127.0.0.1:3000',\n                    process.env.FRONTEND_URL\n                ].filter(Boolean);\n                \n                if (allowedOrigins.includes(origin)) {\n                    callback(null, true);\n                } else {\n                    callback(null, true); // Allow all origins in development\n                }\n            },\n            credentials: true,\n            methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],\n            allowedHeaders: ['Content-Type', 'Authorization', 'X-API-Key', 'X-Requested-With']\n        }));\n\n        // Compression\n        this.app.use(compression());\n\n        // Logging\n        this.app.use(morgan('combined'));\n\n        // Rate limiting\n        const limiter = rateLimit({\n            windowMs: 15 * 60 * 1000, // 15 minutes\n            max: 200, // Increased limit\n            message: {\n                error: 'Too many requests',\n                retryAfter: '15 minutes'\n            },\n            standardHeaders: true,\n            legacyHeaders: false,\n            skip: (req) => {\n                // Skip rate limiting for health checks\n                return req.path === '/health' || req.path === '/api/health';\n            }\n        });\n        this.app.use(limiter);\n\n        // Body parsing\n        this.app.use(express.json({ limit: '10mb' }));\n        this.app.use(express.urlencoded({ extended: true, limit: '10mb' }));\n\n        // Request logging and timing\n        this.app.use((req, res, next) => {\n            req.startTime = Date.now();\n            const originalSend = res.send;\n            \n            res.send = function(data) {\n                res.responseTime = Date.now() - req.startTime;\n                return originalSend.call(this, data);\n            };\n            \n            next();\n        });\n    }\n\n    setupRoutes() {\n        // Health check with detailed status\n        this.app.get('/health', async (req, res) => {\n            try {\n                const dockerStatus = await this.docker.ping();\n                const servicesCount = this.serviceCache.size;\n                \n                res.json({\n                    status: 'healthy',\n                    timestamp: new Date().toISOString(),\n                    version: process.env.npm_package_version || '1.0.0',\n                    uptime: process.uptime(),\n                    memory: process.memoryUsage(),\n                    docker: dockerStatus ? 'connected' : 'disconnected',\n                    services: servicesCount,\n                    clients: this.wsClients.size\n                });\n            } catch (error) {\n                res.status(503).json({\n                    status: 'unhealthy',\n                    error: error.message,\n                    timestamp: new Date().toISOString()\n                });\n            }\n        });\n\n        // API documentation\n        this.app.get('/api/docs', (req, res) => {\n            res.json(this.getAPIDocumentation());\n        });\n\n        // Service management routes\n        this.setupServiceRoutes();\n        \n        // Docker management routes\n        this.setupDockerRoutes();\n        \n        // System monitoring routes\n        this.setupMonitoringRoutes();\n        \n        // Configuration routes\n        this.setupConfigRoutes();\n        \n        // Log management routes\n        this.setupLogRoutes();\n    }\n\n    setupServiceRoutes() {\n        const router = express.Router();\n\n        // Get all services with caching\n        router.get('/services', async (req, res, next) => {\n            try {\n                const { refresh = false } = req.query;\n                \n                if (refresh === 'true' || this.serviceCache.size === 0) {\n                    await this.discoverServices();\n                }\n                \n                const services = Object.fromEntries(this.serviceCache);\n                \n                res.json({\n                    success: true,\n                    data: {\n                        services,\n                        total: this.serviceCache.size,\n                        lastUpdated: new Date().toISOString()\n                    },\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        // Get specific service status\n        router.get('/services/:service/status', async (req, res, next) => {\n            try {\n                const { service } = req.params;\n                const status = await this.getServiceStatus(service);\n                \n                res.json({\n                    success: true,\n                    data: status,\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        // Service health check\n        router.get('/services/:service/health', async (req, res, next) => {\n            try {\n                const { service } = req.params;\n                const health = await this.checkServiceHealth(service, true);\n                \n                res.json({\n                    success: true,\n                    data: health,\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        // Control service (start/stop/restart)\n        router.post('/services/:service/:action', async (req, res, next) => {\n            try {\n                const { service, action } = req.params;\n                const { timeout = 30 } = req.body;\n                \n                // Validate action\n                const validActions = ['start', 'stop', 'restart'];\n                if (!validActions.includes(action)) {\n                    return res.status(400).json({\n                        success: false,\n                        error: `Invalid action: ${action}`,\n                        validActions,\n                        timestamp: new Date().toISOString()\n                    });\n                }\n\n                const result = await this.controlService(service, action, { timeout });\n                \n                // Broadcast to WebSocket clients\n                this.io.emit('service-action', {\n                    service,\n                    action,\n                    result,\n                    timestamp: new Date().toISOString()\n                });\n                \n                res.json({\n                    success: true,\n                    data: result,\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        // Get service logs\n        router.get('/services/:service/logs', async (req, res, next) => {\n            try {\n                const { service } = req.params;\n                const { lines = 100, since, until, follow = false } = req.query;\n                \n                const logs = await this.getServiceLogs(service, {\n                    lines: parseInt(lines),\n                    since,\n                    until,\n                    follow: follow === 'true'\n                });\n                \n                res.json({\n                    success: true,\n                    data: { logs, service },\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        // Bulk service operations\n        router.post('/services/bulk/:action', async (req, res, next) => {\n            try {\n                const { action } = req.params;\n                const { services, timeout = 30 } = req.body;\n                \n                const validActions = ['start', 'stop', 'restart'];\n                if (!validActions.includes(action)) {\n                    return res.status(400).json({\n                        success: false,\n                        error: `Invalid action: ${action}`,\n                        validActions,\n                        timestamp: new Date().toISOString()\n                    });\n                }\n\n                const results = await this.bulkServiceOperation(services, action, { timeout });\n                \n                // Broadcast to WebSocket clients\n                this.io.emit('bulk-service-action', {\n                    services,\n                    action,\n                    results,\n                    timestamp: new Date().toISOString()\n                });\n                \n                res.json({\n                    success: true,\n                    data: results,\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        this.app.use('/api', router);\n    }\n\n    setupDockerRoutes() {\n        const router = express.Router();\n\n        // Docker system info\n        router.get('/docker/info', async (req, res, next) => {\n            try {\n                const info = await this.docker.info();\n                const version = await this.docker.version();\n                \n                res.json({\n                    success: true,\n                    data: {\n                        info: {\n                            containers: info.Containers,\n                            images: info.Images,\n                            serverVersion: info.ServerVersion,\n                            kernelVersion: info.KernelVersion,\n                            operatingSystem: info.OperatingSystem,\n                            totalMemory: info.MemTotal,\n                            cpus: info.NCPU\n                        },\n                        version\n                    },\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        // List all containers\n        router.get('/docker/containers', async (req, res, next) => {\n            try {\n                const { all = true } = req.query;\n                const containers = await this.docker.listContainers({ all: all === 'true' });\n                \n                const containerData = containers.map(container => ({\n                    id: container.Id.substring(0, 12),\n                    name: container.Names[0]?.replace('/', ''),\n                    image: container.Image,\n                    state: container.State,\n                    status: container.Status,\n                    ports: container.Ports,\n                    created: container.Created,\n                    labels: container.Labels\n                }));\n                \n                res.json({\n                    success: true,\n                    data: {\n                        containers: containerData,\n                        total: containers.length\n                    },\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        // Docker stats\n        router.get('/docker/stats', async (req, res, next) => {\n            try {\n                const containers = await this.docker.listContainers();\n                const statsPromises = containers.slice(0, 10).map(async (containerInfo) => {\n                    try {\n                        const container = this.docker.getContainer(containerInfo.Id);\n                        const stats = await container.stats({ stream: false });\n                        \n                        return {\n                            id: containerInfo.Id.substring(0, 12),\n                            name: containerInfo.Names[0]?.replace('/', ''),\n                            cpuPercent: this.calculateCPUPercent(stats),\n                            memoryUsed: stats.memory_stats.usage,\n                            memoryLimit: stats.memory_stats.limit,\n                            memoryPercent: (stats.memory_stats.usage / stats.memory_stats.limit) * 100,\n                            networkRx: stats.networks?.eth0?.rx_bytes || 0,\n                            networkTx: stats.networks?.eth0?.tx_bytes || 0\n                        };\n                    } catch (error) {\n                        return {\n                            id: containerInfo.Id.substring(0, 12),\n                            name: containerInfo.Names[0]?.replace('/', ''),\n                            error: error.message\n                        };\n                    }\n                });\n                \n                const stats = await Promise.all(statsPromises);\n                \n                res.json({\n                    success: true,\n                    data: { stats },\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        this.app.use('/api', router);\n    }\n\n    setupMonitoringRoutes() {\n        const router = express.Router();\n\n        // System overview\n        router.get('/system/overview', async (req, res, next) => {\n            try {\n                const overview = {\n                    timestamp: new Date().toISOString(),\n                    uptime: process.uptime(),\n                    memory: process.memoryUsage(),\n                    cpu: process.cpuUsage(),\n                    services: {\n                        total: this.serviceCache.size,\n                        running: 0,\n                        stopped: 0,\n                        healthy: 0,\n                        unhealthy: 0\n                    },\n                    connections: {\n                        websocket: this.wsClients.size,\n                        docker: 'connected'\n                    }\n                };\n\n                // Count service states\n                for (const [name, service] of this.serviceCache) {\n                    if (service.state === 'running') overview.services.running++;\n                    else overview.services.stopped++;\n                    \n                    if (service.healthy === true) overview.services.healthy++;\n                    else if (service.healthy === false) overview.services.unhealthy++;\n                }\n                \n                res.json({\n                    success: true,\n                    data: overview,\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        this.app.use('/api', router);\n    }\n\n    setupConfigRoutes() {\n        const router = express.Router();\n\n        // Get environment configuration (masked)\n        router.get('/config/env', async (req, res, next) => {\n            try {\n                const envVars = {};\n                const sensitiveKeys = ['password', 'secret', 'key', 'token'];\n                \n                for (const [key, value] of Object.entries(process.env)) {\n                    if (key.startsWith('npm_') || key.startsWith('NODE_')) continue;\n                    \n                    const isSensitive = sensitiveKeys.some(sensitive => \n                        key.toLowerCase().includes(sensitive)\n                    );\n                    \n                    envVars[key] = isSensitive ? '***MASKED***' : value;\n                }\n                \n                res.json({\n                    success: true,\n                    data: { environment: envVars },\n                    responseTime: res.responseTime,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                next(error);\n            }\n        });\n\n        this.app.use('/api', router);\n    }\n\n    setupLogRoutes() {\n        const router = express.Router();\n\n        // Stream logs endpoint info\n        router.get('/logs/stream', (req, res) => {\n            res.json({\n                success: true,\n                message: 'Use WebSocket connection for log streaming',\n                endpoints: {\n                    websocket: `ws://localhost:${this.port}`,\n                    subscribe: 'subscribe-logs'\n                },\n                timestamp: new Date().toISOString()\n            });\n        });\n\n        this.app.use('/api', router);\n    }\n\n    // Service discovery and management methods\n    async discoverServices() {\n        try {\n            const containers = await this.docker.listContainers({ all: true });\n            const mediaServices = [\n                'jellyfin', 'plex', 'emby', 'sonarr', 'radarr', 'lidarr',\n                'bazarr', 'prowlarr', 'qbittorrent', 'transmission', 'sabnzbd',\n                'overseerr', 'jellyseerr', 'ombi', 'tautulli', 'portainer',\n                'nginx-proxy-manager', 'grafana', 'prometheus'\n            ];\n\n            this.serviceCache.clear();\n\n            for (const container of containers) {\n                const containerName = container.Names[0]?.replace('/', '') || '';\n                const matchedService = mediaServices.find(service => \n                    containerName.toLowerCase().includes(service.toLowerCase())\n                );\n\n                if (matchedService) {\n                    const serviceData = {\n                        containerId: container.Id.substring(0, 12),\n                        containerName,\n                        image: container.Image,\n                        state: container.State,\n                        status: container.Status,\n                        ports: container.Ports,\n                        created: container.Created,\n                        labels: container.Labels,\n                        lastUpdated: new Date().toISOString()\n                    };\n\n                    // Add health check if running\n                    if (container.State === 'running') {\n                        try {\n                            const health = await this.checkServiceHealth(matchedService);\n                            serviceData.healthy = health.healthy;\n                            serviceData.responseTime = health.responseTime;\n                            serviceData.httpStatus = health.httpStatus;\n                        } catch (error) {\n                            serviceData.healthy = false;\n                            serviceData.healthError = error.message;\n                        }\n                    } else {\n                        serviceData.healthy = false;\n                    }\n\n                    this.serviceCache.set(matchedService, serviceData);\n                }\n            }\n\n            console.log(`🔍 Discovered ${this.serviceCache.size} media services`);\n        } catch (error) {\n            console.error('Service discovery failed:', error.message);\n            throw error;\n        }\n    }\n\n    async getServiceStatus(serviceName) {\n        const service = this.serviceCache.get(serviceName);\n        if (!service) {\n            throw new Error(`Service ${serviceName} not found`);\n        }\n\n        // Refresh container status\n        try {\n            const container = this.docker.getContainer(service.containerId);\n            const data = await container.inspect();\n            \n            const status = {\n                ...service,\n                detailedState: {\n                    status: data.State.Status,\n                    running: data.State.Running,\n                    paused: data.State.Paused,\n                    restarting: data.State.Restarting,\n                    dead: data.State.Dead,\n                    startedAt: data.State.StartedAt,\n                    finishedAt: data.State.FinishedAt\n                },\n                restartCount: data.RestartCount,\n                networkSettings: data.NetworkSettings\n            };\n\n            // Update cache\n            this.serviceCache.set(serviceName, { ...service, ...status });\n            \n            return status;\n        } catch (error) {\n            throw new Error(`Failed to get status for ${serviceName}: ${error.message}`);\n        }\n    }\n\n    async checkServiceHealth(serviceName, detailed = false) {\n        const serviceEndpoints = {\n            jellyfin: { port: 8096, path: '/health' },\n            plex: { port: 32400, path: '/identity' },\n            emby: { port: 8097, path: '/health' },\n            sonarr: { port: 8989, path: '/api/v3/system/status' },\n            radarr: { port: 7878, path: '/api/v3/system/status' },\n            lidarr: { port: 8686, path: '/api/v1/system/status' },\n            bazarr: { port: 6767, path: '/api/system/status' },\n            prowlarr: { port: 9696, path: '/api/v1/system/status' },\n            qbittorrent: { port: 8080, path: '/api/v2/app/version' },\n            transmission: { port: 9091, path: '/transmission/rpc' },\n            sabnzbd: { port: 8081, path: '/sabnzbd/api?mode=version' },\n            overseerr: { port: 5055, path: '/api/v1/status' },\n            jellyseerr: { port: 5055, path: '/api/v1/status' },\n            ombi: { port: 3579, path: '/api/v1/status' },\n            tautulli: { port: 8181, path: '/api/v2?cmd=get_server_info' },\n            portainer: { port: 9000, path: '/api/status' },\n            'nginx-proxy-manager': { port: 81, path: '/api/nginx/proxy-hosts' },\n            grafana: { port: 3000, path: '/api/health' },\n            prometheus: { port: 9090, path: '/-/healthy' }\n        };\n\n        const endpoint = serviceEndpoints[serviceName];\n        if (!endpoint) {\n            return { healthy: false, error: 'Unknown service endpoint' };\n        }\n\n        try {\n            const startTime = Date.now();\n            const response = await axios.get(\n                `http://localhost:${endpoint.port}${endpoint.path}`,\n                { \n                    timeout: 10000,\n                    validateStatus: (status) => status < 500,\n                    headers: {\n                        'User-Agent': 'Media-API-Health-Check/1.0'\n                    }\n                }\n            );\n            const responseTime = Date.now() - startTime;\n\n            const result = {\n                healthy: true,\n                httpStatus: response.status,\n                responseTime,\n                lastCheck: new Date().toISOString()\n            };\n\n            if (detailed) {\n                result.endpoint = `http://localhost:${endpoint.port}${endpoint.path}`;\n                result.headers = response.headers;\n                result.dataSize = JSON.stringify(response.data).length;\n            }\n\n            return result;\n        } catch (error) {\n            return {\n                healthy: false,\n                error: error.code || error.message,\n                responseTime: error.response ? Date.now() - Date.now() : null,\n                httpStatus: error.response?.status,\n                lastCheck: new Date().toISOString()\n            };\n        }\n    }\n\n    async controlService(serviceName, action, options = {}) {\n        const service = this.serviceCache.get(serviceName);\n        if (!service) {\n            throw new Error(`Service ${serviceName} not found`);\n        }\n\n        const { timeout = 30 } = options;\n        const container = this.docker.getContainer(service.containerId);\n\n        try {\n            switch (action) {\n                case 'start':\n                    await container.start();\n                    break;\n                case 'stop':\n                    await container.stop({ t: timeout });\n                    break;\n                case 'restart':\n                    await container.restart({ t: timeout });\n                    break;\n                default:\n                    throw new Error(`Invalid action: ${action}`);\n            }\n\n            // Update cache after successful operation\n            setTimeout(() => this.discoverServices(), 2000);\n\n            return {\n                success: true,\n                service: serviceName,\n                action,\n                containerId: service.containerId,\n                message: `Service ${serviceName} ${action} completed successfully`,\n                timestamp: new Date().toISOString()\n            };\n        } catch (error) {\n            if (error.statusCode === 304) {\n                return {\n                    success: true,\n                    service: serviceName,\n                    action,\n                    message: `Service ${serviceName} already in desired state`,\n                    timestamp: new Date().toISOString()\n                };\n            }\n            throw error;\n        }\n    }\n\n    async bulkServiceOperation(services, action, options = {}) {\n        const results = {};\n        const promises = services.map(async (serviceName) => {\n            try {\n                const result = await this.controlService(serviceName, action, options);\n                results[serviceName] = result;\n            } catch (error) {\n                results[serviceName] = {\n                    success: false,\n                    error: error.message,\n                    service: serviceName,\n                    action,\n                    timestamp: new Date().toISOString()\n                };\n            }\n        });\n\n        await Promise.all(promises);\n        return results;\n    }\n\n    async getServiceLogs(serviceName, options = {}) {\n        const service = this.serviceCache.get(serviceName);\n        if (!service) {\n            throw new Error(`Service ${serviceName} not found`);\n        }\n\n        const {\n            lines = 100,\n            since = null,\n            until = null,\n            follow = false\n        } = options;\n\n        try {\n            const container = this.docker.getContainer(service.containerId);\n            const logOptions = {\n                stdout: true,\n                stderr: true,\n                tail: lines,\n                timestamps: true,\n                follow\n            };\n\n            if (since) logOptions.since = Math.floor(new Date(since).getTime() / 1000);\n            if (until) logOptions.until = Math.floor(new Date(until).getTime() / 1000);\n\n            const logs = await container.logs(logOptions);\n            return logs.toString('utf8');\n        } catch (error) {\n            throw new Error(`Failed to get logs for ${serviceName}: ${error.message}`);\n        }\n    }\n\n    calculateCPUPercent(stats) {\n        const cpuDelta = stats.cpu_stats.cpu_usage.total_usage - stats.precpu_stats.cpu_usage.total_usage;\n        const systemDelta = stats.cpu_stats.system_cpu_usage - stats.precpu_stats.system_cpu_usage;\n        const numCPUs = stats.cpu_stats.online_cpus || 1;\n        \n        if (systemDelta > 0 && cpuDelta > 0) {\n            return (cpuDelta / systemDelta) * numCPUs * 100;\n        }\n        return 0;\n    }\n\n    // Socket.IO setup\n    setupSocketIO() {\n        this.io.on('connection', (socket) => {\n            console.log(`🔌 WebSocket client connected: ${socket.id}`);\n            \n            // Store client\n            this.wsClients.set(socket.id, {\n                socket,\n                connectedAt: new Date().toISOString(),\n                subscriptions: new Set()\n            });\n\n            // Send initial service status\n            this.sendInitialStatus(socket);\n\n            // Handle client events\n            this.setupSocketHandlers(socket);\n\n            socket.on('disconnect', (reason) => {\n                console.log(`📱 WebSocket client disconnected: ${socket.id} (${reason})`);\n                this.wsClients.delete(socket.id);\n            });\n        });\n\n        // Start real-time monitoring\n        this.startRealTimeMonitoring();\n    }\n\n    setupSocketHandlers(socket) {\n        const client = this.wsClients.get(socket.id);\n\n        socket.on('subscribe-services', () => {\n            client.subscriptions.add('services');\n            socket.emit('subscription-confirmed', {\n                type: 'services',\n                message: 'Subscribed to service updates'\n            });\n        });\n\n        socket.on('subscribe-logs', (data) => {\n            const service = data?.service || 'all';\n            client.subscriptions.add(`logs-${service}`);\n            socket.emit('subscription-confirmed', {\n                type: 'logs',\n                service,\n                message: `Subscribed to logs for ${service}`\n            });\n        });\n\n        socket.on('get-services', async () => {\n            try {\n                const services = Object.fromEntries(this.serviceCache);\n                socket.emit('services-data', {\n                    services,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                socket.emit('error', {\n                    message: error.message,\n                    timestamp: new Date().toISOString()\n                });\n            }\n        });\n\n        socket.on('control-service', async (data) => {\n            try {\n                const { service, action, options = {} } = data;\n                const result = await this.controlService(service, action, options);\n                \n                socket.emit('service-control-result', result);\n                \n                // Broadcast to all subscribers\n                this.broadcast('service-action', {\n                    service,\n                    action,\n                    result,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                socket.emit('error', {\n                    message: error.message,\n                    action: data.action,\n                    service: data.service,\n                    timestamp: new Date().toISOString()\n                });\n            }\n        });\n\n        socket.on('get-logs', async (data) => {\n            try {\n                const { service, options = {} } = data;\n                const logs = await this.getServiceLogs(service, options);\n                \n                socket.emit('logs-data', {\n                    service,\n                    logs,\n                    timestamp: new Date().toISOString()\n                });\n            } catch (error) {\n                socket.emit('error', {\n                    message: error.message,\n                    timestamp: new Date().toISOString()\n                });\n            }\n        });\n\n        socket.on('ping', () => {\n            socket.emit('pong', {\n                timestamp: new Date().toISOString()\n            });\n        });\n    }\n\n    async sendInitialStatus(socket) {\n        try {\n            const services = Object.fromEntries(this.serviceCache);\n            socket.emit('initial-status', {\n                services,\n                clientCount: this.wsClients.size,\n                timestamp: new Date().toISOString()\n            });\n        } catch (error) {\n            socket.emit('error', {\n                message: error.message,\n                timestamp: new Date().toISOString()\n            });\n        }\n    }\n\n    broadcast(event, data) {\n        this.io.emit(event, data);\n    }\n\n    startRealTimeMonitoring() {\n        // Monitor service status every 30 seconds\n        this.monitoringInterval = setInterval(async () => {\n            try {\n                await this.discoverServices();\n                \n                // Broadcast updated service status to subscribers\n                const services = Object.fromEntries(this.serviceCache);\n                this.wsClients.forEach((client) => {\n                    if (client.subscriptions.has('services')) {\n                        client.socket.emit('services-update', {\n                            services,\n                            timestamp: new Date().toISOString()\n                        });\n                    }\n                });\n            } catch (error) {\n                console.error('Real-time monitoring error:', error.message);\n            }\n        }, 30000);\n\n        console.log('🔄 Real-time monitoring started');\n    }\n\n    stopRealTimeMonitoring() {\n        if (this.monitoringInterval) {\n            clearInterval(this.monitoringInterval);\n            this.monitoringInterval = null;\n            console.log('⏹️ Real-time monitoring stopped');\n        }\n    }\n\n    getAPIDocumentation() {\n        return {\n            title: 'Enhanced Media Server API',\n            version: '2.0.0',\n            description: 'Comprehensive API for managing media server infrastructure with real-time WebSocket support',\n            baseUrl: `http://localhost:${this.port}/api`,\n            websocket: `ws://localhost:${this.port}`,\n            endpoints: {\n                services: {\n                    'GET /services': 'Get all discovered services',\n                    'GET /services/:service/status': 'Get detailed service status',\n                    'GET /services/:service/health': 'Get service health check',\n                    'POST /services/:service/:action': 'Control service (start/stop/restart)',\n                    'GET /services/:service/logs': 'Get service logs',\n                    'POST /services/bulk/:action': 'Bulk service operations'\n                },\n                docker: {\n                    'GET /docker/info': 'Get Docker system information',\n                    'GET /docker/containers': 'List all containers',\n                    'GET /docker/stats': 'Get container statistics'\n                },\n                system: {\n                    'GET /system/overview': 'Get system overview'\n                },\n                config: {\n                    'GET /config/env': 'Get environment configuration'\n                },\n                logs: {\n                    'GET /logs/stream': 'WebSocket log streaming info'\n                }\n            },\n            websocketEvents: {\n                client: [\n                    'subscribe-services',\n                    'subscribe-logs',\n                    'get-services',\n                    'control-service',\n                    'get-logs',\n                    'ping'\n                ],\n                server: [\n                    'initial-status',\n                    'services-update',\n                    'service-action',\n                    'service-control-result',\n                    'logs-data',\n                    'subscription-confirmed',\n                    'error',\n                    'pong'\n                ]\n            }\n        };\n    }\n\n    setupErrorHandling() {\n        // 404 handler\n        this.app.use('*', (req, res) => {\n            res.status(404).json({\n                success: false,\n                error: 'Endpoint not found',\n                path: req.originalUrl,\n                method: req.method,\n                availableEndpoints: '/api/docs',\n                timestamp: new Date().toISOString()\n            });\n        });\n\n        // Global error handler\n        this.app.use((error, req, res, next) => {\n            console.error('API Error:', error.message);\n            \n            const errorResponse = {\n                success: false,\n                error: error.message,\n                path: req.originalUrl,\n                method: req.method,\n                timestamp: new Date().toISOString()\n            };\n\n            // Add stack trace in development\n            if (process.env.NODE_ENV !== 'production') {\n                errorResponse.stack = error.stack;\n            }\n\n            res.status(error.statusCode || 500).json(errorResponse);\n        });\n    }\n\n    async start() {\n        try {\n            // Initialize the API\n            await this.initialize();\n\n            // Start the server\n            this.server.listen(this.port, () => {\n                console.log(`🚀 Enhanced Media Server API started on port ${this.port}`);\n                console.log(`📚 API Documentation: http://localhost:${this.port}/api/docs`);\n                console.log(`🔌 WebSocket: ws://localhost:${this.port}`);\n                console.log(`💾 Services discovered: ${this.serviceCache.size}`);\n                console.log(`🌐 Environment: ${process.env.NODE_ENV || 'development'}`);\n            });\n\n            // Graceful shutdown\n            process.on('SIGTERM', () => this.shutdown());\n            process.on('SIGINT', () => this.shutdown());\n\n        } catch (error) {\n            console.error('Failed to start Enhanced Media Server API:', error.message);\n            process.exit(1);\n        }\n    }\n\n    async shutdown() {\n        console.log('🛑 Shutting down Enhanced Media Server API...');\n        \n        // Stop monitoring\n        this.stopRealTimeMonitoring();\n\n        // Close WebSocket connections\n        this.wsClients.forEach((client) => {\n            client.socket.disconnect(true);\n        });\n\n        // Close server\n        this.server.close(() => {\n            console.log('✅ Enhanced Media Server API shut down complete');\n            process.exit(0);\n        });\n    }\n}\n\n// Start the server if this file is run directly\nif (require.main === module) {\n    const api = new EnhancedMediaServerAPI();\n    api.start().catch(console.error);\n}\n\nmodule.exports = EnhancedMediaServerAPI;